// src/config/live.ts
export const LIVE_CONFIG = {
  // Id do canal no YouTube (use o ID, não o handle). Ex.: UCxxxxxxxxxxxxxxxx
  channelId: 'UCT0uZ2_HNJETRhS4Rj_BXdA',

  // (opcional) handle do canal para abrir no app YouTube / web (fallback)
  // Ex.: '@canalDaIBI'
  channelHandle: '@ibidentidade',

  // Caso já saiba o ID do vídeo da live atual (quando houver), dá pra forçar:
  // liveVideoId: 'M7lc1UVf-VE',
};